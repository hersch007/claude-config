---
name: project-kpi-abstraction-needed
description: Intelligence/AI dashboard KPI hook (sp_intel_summary_lines) built, deployed, and verified live on SMTI (2026-07-04) — service-request stage/aging/high-priority dashboard added smti-service 1.2.3 (2026-07-06); Fruth sqs 8.9.9 brought to full parity (aging card+banner+footer, AI module installed) 2026-07-06; Fruth AI Business Summary WORKING via intelligence 1.2.0 api-key fallback — all 3 sites on 1.2.0
metadata: 
  node_type: memory
  type: project
  originSessionId: bce3fcb0-21e9-482d-b4c5-fd9cda46bc99
---

Dashboard stat-card widgets already have real extension points (`sp_dashboard_after_stats`, `sp_dashboard_after_grid` in `start-performance/templates/views/dashboard.php`) — addons hook in fine.

**Update 2026-07-06 — the Intelligence Core KPI DASHBOARD PAGE now has a provider hook too (`sp_intel_kpi_cards`).** Previously `intelligence.php` read the "Pipeline Value / quotes" cards straight from the local `sp_smti_quotes` table (and tickets from `sp_tickets`). On SMTI those are the wrong source — smti-sales/smti-service data lives in HubSpot — so after the orphan cleanup the KPI page showed a stale $91,510 / 1 quote / no service while the main dashboard + AI summary (already HubSpot-live) showed $1,858,675 / 50 / 4. Fixed with `apply_filters('sp_intel_kpi_cards', array(), $ctx)` (each card = `array('label','value'(preformatted string),'sub','accent'=>accent|warn|danger|green)`); removed the hardcoded `sp_smti_quotes` reads + stale "Quote Status Breakdown" panel from `intelligence.php`. Providers: `sp_smti_sales_intel_kpi_cards` (smti-sales, 1.3.6 — Open Quotes + Pipeline Value via live `sp_smti_sales_get_open_pipeline()`) and `sp_smti_intel_kpi_cards` (smti-service, 1.2.2 — Service Requests via live HubSpot ticket search). Intelligence Core bumped to 1.1.6. Verified live: all three surfaces (main dashboard, AI summary, KPI dashboard) now agree. Core-tickets KPI card + Ticket Health panel still read `sp_tickets` (correct — that's a genuinely-local core module; gated on sp-tickets active, absent on SMTI). Follow-up ideas: core `sp-sales` could also register `sp_intel_kpi_cards` (it never had KPI-page cards); a live-HubSpot stage-breakdown PANEL hook could replace the removed Quote Status Breakdown.

**Update 2026-07-06 (intelligence 1.1.7) — per-site super-admin control over which CORE CRM metrics show on the KPI board AND feed the AI Summary.** Problem: on SMTI the built-in CRM metrics (Contacts/Companies/Leads/Tasks) are always 0 (real data is in HubSpot), so they cluttered the KPI board AND poisoned the AI Summary — it reasoned off the zeros producing nonsense like "0 active leads = you're generating quotes from nowhere." Fix: option `sp_intel_core_kpis` (array of enabled core-metric keys; unset/default = all shown, so Core/Fruth unchanged). New helpers `sp_intel_core_kpi_keys()` + `sp_intel_core_kpi_enabled($key)`. Super-admin checkboxes added to the Intelligence Core AI-Settings form (`sp_intel_settings_section`/`sp_intel_save_settings`). Gated: the 4 core KPI cards + the Lead Pipeline funnel / New Contacts trend / Lead Status Breakdown sections in `intelligence.php` (Recent Activity always shows, becomes full-width via a new `.sp-intel-row .sp-intel-section:only-child` CSS rule), AND the contacts/leads/tasks baseline lines in `sp_intel_ajax_ai_insights`. **Applied on SMTI: `sp_intel_core_kpis` set to `[]` (all core hidden)** so its KPI board + AI summary now show ONLY the live custom-module data (Quotes 50/$1.86M via smti-sales, Service Requests 4 via smti-service). Verified the AI prompt on SMTI now contains only the two provider lines, no core zeros. Richard controls this per-site via Settings → the AI-Settings section checkboxes.

**Update 2026-07-06 (intelligence 1.1.9) — auto-hide-when-empty is now the DEFAULT.** Two modes for `sp_intel_core_kpi_enabled($key)`: (1) EXPLICIT — option `sp_intel_core_kpis` is a saved array → show exactly those (super admin's choice wins); (2) AUTO — option unset → show a metric only if its table has ≥1 row (`sp_intel_core_kpi_has_data($key)`, static-cached). So a NEW client with an empty CRM auto-hides the empty core metrics with zero config, and a metric appears the moment it has data. Settings UI got an "Auto — hide any metric that has no data (recommended)" checkbox (`sp_intel_core_kpis_auto`); when checked, save `delete_option`s (auto); unchecked saves the explicit array; JS disables the per-metric boxes while Auto is on. Helper `sp_intel_core_kpi_is_auto()`. Verified: sp (all core tables have data) = AUTO shows all; SMTI + Fruth remain EXPLICIT `[]` (all hidden — left as-is, and their tables are empty so AUTO would hide them too).

**Update 2026-07-06 (intelligence 1.1.8 + smti-sales 1.3.7) — Top Distributors scorecard + a PANEL hook.** Added `do_action('sp_intel_after_kpi')` at the end of `intelligence.php` (the panel-provider hook alongside the earlier card-provider `sp_intel_kpi_cards`) so modules can append detail panels below the KPI grid, reusing the page's `.sp-intel-section`/`.sp-intel-stat-row` styling. smti-sales registers `sp_smti_sales_intel_distributor_panel` → renders a "Top Distributors — Open Pipeline" scorecard (rank / distributor / N quotes / $value, top 12, ranked by value desc). Data from new `sp_smti_sales_get_distributor_scorecard()` which aggregates live open HubSpot deals by the `smt_distributor_name` property (verified 100% coverage — all 50 open deals have it; 8 distinct distributors, e.g. Jake McGregor Consulting Group 17 quotes/$630K leading). uasort uses PHP-5.6-safe comparison (not `<=>`). Panel skips if smti-sales nav is hidden. Verified live: no fatal, hook registered, aggregation correct.

**Update 2026-07-06 (smti-sales 1.3.8):** (a) Made the scorecard `<h3>` bolder per Richard — inline style overriding the muted `.sp-intel-section h3` (now 1.15rem / weight 800 / dark #0f172a / no uppercase). (b) Added aging-quotes-by-distributor to the AI summary: new `sp_smti_sales_get_aging_by_distributor($age_days=30)` aggregates OPEN HubSpot deals created >30 days ago by `smt_distributor_name`; `sp_smti_sales_intel_summary_lines` now appends a "- Aging open quotes still unclosed after 30+ days, by distributor (follow-up candidates): Dist (N quotes, $V); ..." line (top 6 by value). This gives the AI concrete follow-up targets so it recommends specifics like "Jake McGregor has 13 quotes worth $409K aging past 30 days — initiate follow-up." Verified live: the aging line appears in the AI prompt with correct data (Jake McGregor 13/$409K leading).

**Update 2026-07-06 (smti-service 1.2.3) — service-request dashboard: stage panel + high-priority + aging.** Added a shared static-cached `sp_smti_service_analytics($age_days=30)` (one HubSpot round-trip/request) that fetches the ticket pipeline stages (label + `metadata.ticketState==='CLOSED'` + displayOrder) and searches tickets (limit 100, props hs_pipeline_stage/hs_ticket_priority/createdate), returning open workload (EXCLUDES the closed state — note SMTI pipeline "0" only marks stage id `4`=Closed as CLOSED; "Resolved" is NOT closed), a labelled `by_stage` breakdown in workflow order, `high_priority_open` (hs_ticket_priority=HIGH), `aging_open` (open & age>=30d), `waiting_customer` (stage label contains "customer"), `oldest_days`. Four surfaces consume it: (1) `sp_intel_kpi_cards` now returns TWO cards — "Service Requests" (open count; warn accent when aging>0) + "High Priority" (danger when >0, green "none open" at 0, ALWAYS shown on KPI board); (2) NEW `sp_intel_after_kpi` panel `sp_smti_service_intel_stage_panel` — "Service Requests by Stage" scorecard (stage / count / % of open, "(stalled on customer)" tag on customer-wait rows, footer strip for high-priority/aging/oldest); (3) `sp_smti_intel_summary_lines` enriched — open + new-in-period + high-priority, a full stage-breakdown line, and an "attention" line for aging-30+/waiting-on-customer; (4) `sp_smti_dashboard_stats` (main dash Custom Metrics) switched total→open and adds a red danger BANNER only when high_priority>0 (auto-hides at 0 — no `sp-stats-3` class exists, banner draws the eye better than a card). PHP-5.6-safe comparators `sp_smti_stage_order_cmp`/`sp_smti_stage_row_cmp` (no spaceship). Deployed + verified live on SMTI: open=5, closed=0, high_priority=0 (green, no banner), stages Awaiting Review 2 / Troubleshooting 1 / Waiting on Customer 1 (stalled) / Scheduled for Service 1, oldest_days=22 (no 30+ aging yet). To demo the urgent state: set a HubSpot ticket to HIGH priority → banner + red card appear. See [[core_platform_architecture]].

**Update 2026-07-06 (Intelligence 1.2.2) — AI summary was truncating the last sentence; raised max_tokens 200→500.** `sp_intel_ajax_ai_insights()` called Anthropic with `max_tokens=200`, but the Status + 2 + 3 lines + Next-move format needs ~190–215 output tokens, so the final "Next move" sentence got cut mid-word (seen on both the KPI Dashboard "AI Smart Summary" AND the main dashboard "AI Business Summary" — they share this one handler, action `sp_intel_ai_insights`). Bumped to 500. Verified live: stop_reason now `end_turn` (was `max_tokens`), Fruth 189 / SP 215 output tokens, both end on a complete sentence. Deployed to all 3.

**Update 2026-07-06 (start-performance-ai 1.3.1) — Pipeline Insights "Not authorized" fix + hide-when-no-leads.** The AI module's dashboard "Pipeline Insights" card (`sp_dashboard_after_grid` → `sp_ai_pipeline_insights_ui`, AJAX `sp_ai_ajax_pipeline_insights`) errored "Error: Not authorized" on SP AND Fruth. Root cause: its auth check was `!sp_get_current_team_member()` with NO super-admin fallback — but Richard logs in as a super-admin who isn't a team-member record (the Intelligence AI Business Summary works precisely because it has the `sp_is_super_admin()` fallback). Fix: new `sp_ai_authed()` helper (team member OR super admin) now used by ALL four AI AJAX handlers (pipeline insights, contact summary, ticket triage, ticket analysis — all had the same latent bug). Separately, Pipeline Insights analyzes the LEAD pipeline (`sp_leads`); on quotes-only Fruth (0 leads) it's irrelevant, so `sp_ai_pipeline_insights_ui()` now returns early when `sp_leads` is empty → card hidden on Fruth, shown on SP. Deployed 1.3.1 to all 3 (sp/fruth active, smti inactive preserved). Verified live: Fruth dashboard no longer shows the card; SP "Refresh Insights" generates fresh analysis ("Generated just now", no auth error). So the answer to "should it be gone?" was: gone on Fruth (no lead pipeline), fixed on SP (real bug).

**Update 2026-07-06 (core 2.5.4 + Fruth sqs 8.9.10) — main-dashboard auto-hide + Quote Builder toolbar restyle.** (1) MAIN DASHBOARD auto-hide (`start-performance/templates/views/dashboard.php`): previously the Core Metrics row, Pipeline funnel, and Recent Activity/Contacts gated only on `sp_is_view_hidden()` (nav visibility), so quotes-only clients (SMTI/Fruth, empty core CRM) showed a row of 0s + empty funnel/lists. Added visibility flags at top of template: `$sp_metric_on($key)` = `sp_intel_core_kpi_enabled($key)` when Intelligence Core is active, else true (soft dependency — plain core install unchanged). Core Metrics heading+row wrapped in `$show_core_metrics`; each card gated on its `$show_*_card`; Pipeline funnel on `$show_pipeline_funnel` (leads metric on AND pipeline sum>0); the whole `.sp-dash-grid` on `$show_dash_grid` (any of funnel/activity/contacts/team). Also switched the dashboard AI-card gate `$has_intel` to use `sp_intel_get_api_key()` (resolver, so it shows when either key is set). Result verified live: SMTI + Fruth dashboards now show ONLY Greeting → Quick Actions → Custom Metrics → AI Summary (empty core stuff gone); SP unaffected (has data → everything shows). (2) FRUTH QUOTE BUILDER TOOLBAR restyle (sales-quote-system.php `.sqs-quote-toolbar`/`.sqs-bar-btn*`, ~line 534): was a dark `#1f2937` band with mismatched white pill buttons; now a white SP card (border #e2e8f0, radius 12, subtle shadow) with SP-style buttons — Save Quote = accent primary via `var(--sp-accent,#CC1F1F)` (inherits Fruth brand), secondaries = `.sp-btn-ghost`-style (#f1f5f9). Verified via screenshot: blends with the metrics/KPI/product cards as one SP view. Deployed core 2.5.4 to all 3, sqs 8.9.10 to Fruth. Fruth's AI summary now reads "Fruth Custom Packaging" (platform name) and fully generates. NOTE (recurring): deploy bursts keep tripping the host SSH rate-limit (port 22 refused; HTTP stays 200). It's a connection-rate block (extends on probing the actual scp), NOT auth — go QUIET ~2 min then finish in minimal connections (scp multiple files in one call + one ssh doing all installs). Fruth's core deploy silently no-op'd mid-block once (stayed 2.5.3) — always re-verify SP_VERSION after a blocked deploy.

**Update 2026-07-06 (Intelligence 1.2.1 + start-performance-ai 1.3.0) — relabeled the two AI-key fields + set Fruth platform name.** After the fallback fix, relabeled both fields so the two-field trap is obvious: Intelligence Core field is now "Anthropic API Key — KPI Dashboard AI Summary" (hint explains it powers the AI Business Summary and falls back to the AI Assistant key when blank); AI module section is now "AI Assistant — Chat & Marketing" with field "AI Assistant API Key" (hint: OpenAI or Anthropic per model, also used as the summary fallback for Anthropic keys only). Set Fruth `sp_platform_name` = "Fruth Custom Packaging" (was empty → prompt had a double space; SP="Start Performance", SMTI="SMTI"). Deployed intelligence 1.2.1 to ALL THREE. ai 1.3.0: SP + Fruth via deploy.ps1 (active); SMTI updated separately via `wp plugin install /tmp/zip --force` WITHOUT --activate (updates files but preserves inactive state — deploy.ps1 force-activates, which would wrongly reactivate SMTI's deliberately-off AI marketing module). Result: all 3 on ai 1.3.0 — active on sp+fruth, inactive on smti. (Pattern for updating an intentionally-inactive plugin without turning it on: `wp plugin install --force` sans --activate.) NOTE: a burst of rapid SSH deploy connections triggered a transient host SSH block (port 22 refused/timeout ~5+ min while HTTP stayed 200) — back off and retry, don't hammer. Final verified state: all 3 on intelligence 1.2.1; ai 1.3.0/active on sp+fruth, 1.2.6/inactive on smti; Richard set Fruth's PRIMARY Anthropic key (all 3 now resolve via `primary`, fallback is just a safety net).

**Update 2026-07-06 (Intelligence Core 1.2.0) — AI-key fallback resolves the two-field trap; Fruth AI summary now works.** Two separate Anthropic-key options exist: `sp_anthropic_api_key` (Intelligence Core's AI Business Summary, set via Settings → "AI & Intelligence Keys" super-admin box) and `sp_ai_api_key` (the `start-performance-ai` marketing module's own super-admin settings box). Both boxes are amber "Super Admin Only" and look nearly identical, so after installing the AI module on Fruth, Richard twice saved Fruth's key into `sp_ai_api_key` while the summary reads `sp_anthropic_api_key` (stayed empty → summary wouldn't generate). Cross-site reality: SP has BOTH set (different keys), SMTI has only `sp_anthropic_api_key`, Fruth had only `sp_ai_api_key`. Fix: new `sp_intel_get_api_key()` returns `sp_anthropic_api_key` if set, else falls back to `sp_ai_api_key`; `sp_intel_ajax_ai_insights()` now calls it instead of reading the option directly. Primary always wins when set, so SP/SMTI behavior is unchanged (verified live: sp/smti resolve via primary, fruth via FALLBACK, all 108 chars). Deployed 1.2.0 to ALL THREE sites (identical code). Verified end-to-end: Fruth summary returns HTTP 200 with a real Status/What's-working/Needs-attention/Next-move analysis of its live quote data (2 Draft Tubing quotes, $6,496). MINOR open polish: Fruth's `sp_platform_name` option is empty → prompt reads "Business performance data for  over the last 30 days" (double space); set it to e.g. "Fruth Custom Packaging" in Settings for a cleaner prompt. Deploy note: `deploy.ps1 -Plugin intelligence -Site <sp|smti|fruth>` works (the `$SITES.ContainsKey()` PowerShell error is the known-harmless one).

**Update 2026-07-06 (Fruth sales-quote-system 8.9.9) — Fruth brought to full parity with SMTI/SP.** Fruth already had the whole provider set (`sp_register_addon('fruth-sales')`, dashboard Custom Metrics, `sp_intel_kpi_cards`, `sp_intel_summary_lines`, `sp_intel_after_kpi` "Quotes by Product Type" panel, `_quote_stats()` latest-rev-per-quote_number, `_aging_quotes()`), and the platform layer already matched (core 2.5.3, intelligence 1.1.9, `sp_intel_core_kpis=[]`, CRM empty). Parity pass added the SMTI-service aging/attention treatment: (1) static-cached `_quote_stats()` (was uncached, called ~4x) and `_aging_quotes($age_days)` (now also returns `oldest_days`); (2) NEW "Needs Follow-up" KPI card — always shown when module has quotes, value=aging count, accent warn when >0 / green "all quotes current" at 0; (3) "Fruth Quotes" card accent flips accent→warn when aging>0 (mirrors SMTI Service Requests); (4) amber dashboard BANNER on Custom Metrics when aging>0 (auto-hides at 0 — Fruth's aging=follow-up so amber #f59e0b, vs SMTI high-priority=red danger); (5) aging footer strip on the product panel (count + oldest-days + value). Aging = latest-rev quotes status<>'Archived' with updated_at older than 30 days. Master source: `Custom Builds/Fruth/plugins/core/extracted/sales-quote-system/sales-quote-system.php` (SEPARATE tree, NOT build.ps1 — zip hand-built via System.IO.Compression with FORWARD-SLASH entry names `sales-quote-system/...`; 3 files: main php + sp-views/fruth-quotes.php + fruth-pricing.php; output `plugins/core/sales-quote-system-vX-fruth.zip`). Deployed via `wp plugin install /tmp/…zip --force` over SSH (deploy.ps1 does NOT handle Fruth). ALSO installed+activated `start-performance-ai` 1.2.9 on Fruth (the AI *marketing page* module — Richard said yes; note the KPI AI *summary* does NOT depend on it, it's self-contained in Intelligence Core calling api.anthropic.com via `sp_anthropic_api_key`). Verified live: 8.9.9 active, cards render (2 quotes / $6,496 / Needs Follow-up 0 green), banner+footer correctly hidden at 0 aging. **OUTSTANDING: `sp_anthropic_api_key` is NOT SET on Fruth** → the AI Business Summary won't generate until Richard pastes the key in Fruth → Settings → AI Insights (I can't enter credentials). Everything else is at parity.

**Status (2026-07-04): built and zipped, not yet deployed.** Added a new `sp_intel_summary_lines` filter — `apply_filters('sp_intel_summary_lines', $summary_lines, $days, $since, $today)` — inside `sp_intel_ajax_ai_insights()` in `start-performance-intelligence/start-performance-intelligence.php` (bumped to v1.1.4). The old hardcoded `SHOW TABLES LIKE` branches for core Tickets/Operations/Knowledge/Sales were moved unchanged into `sp_intel_default_summary_lines()`, registered at priority 10 (preserves existing behavior for any install running core modules). The `sp_smti_quotes` branch was removed from Intelligence Core entirely and moved into `start-performance-smti-sales.php` (`sp_smti_sales_intel_summary_lines`, v1.2.7, priority 20). `start-performance-smti-service.php` (v1.1.9, priority 20) got a brand-new `sp_smti_intel_summary_lines` provider that calls the HubSpot ticket search API directly via the existing `sp_smti_hs_request()` helper — this is the fix for the previously-invisible SMTI service-ticket data (Intelligence Core's `sp_tickets` check was always false there since smti-service has no local DB tables).

All three zips (`start-performance-intelligence-1.1.4.zip`, `start-performance-smti-sales-1.2.7.zip`, `start-performance-smti-service-1.1.9.zip`) were deployed to the live SMTI site via `deploy.ps1` on 2026-07-04 and verified active via `wp plugin list` over SSH. Deployment used `deploy.ps1 -Plugin <key> -Site smti` for each — note `deploy.ps1` was missing a proper entry for `start-performance-smti-service` (it only had a `smti-tickets` key pointing at the unrelated legacy `smti-service-tickets` plugin in `current-plugins/`); added a `smti-service` key pointing at `start-performance-smti-service-` in `plugin/`. Also note: `deploy.ps1`'s `$SITES.ContainsKey()` check throws a non-terminating PowerShell error (OrderedDictionary method resolution issue) that can make the deploy command's exit code look like a failure even when the deploy succeeded — always verify via `wp plugin list --path=<site>` over SSH rather than trusting the script's exit code alone.

Next step: manually check the AI Business Summary on SMTI's live dashboard to confirm it now shows a service-ticket bullet alongside the quotes bullet (haven't done this browser-level check yet, only confirmed the plugin files are deployed and active).

**Why:** A second client, Fruth (added 2026-07-04, https://startwebservicesbackup.com/fruth/sp-login/), is coming online. Fruth runs core `start-performance-sales` PLUS a legacy standalone plugin `sales-quote-system` side by side, with its own tables `sqs_quotes`, `sqs_quote_items`, `sqs_pricing_data` (confirmed in `current-plugins/qb-core-extract/sales-quote-system/sales-quote-system.php:141,146,151`) — a third distinct schema, separate from core Sales and from `smti-sales`. That plugin doesn't use `sp_register_addon` at all, so wiring its own `sp_intel_summary_lines` provider is a bigger lift than SMTI's — not yet started.

**How to apply:** When a new client module needs to feed the AI Business Summary, register a callback on `sp_intel_summary_lines` (see `sp_smti_sales_intel_summary_lines` or `sp_smti_intel_summary_lines` as templates) rather than editing `sp_intel_ajax_ai_insights` directly. See [[core_platform_architecture]] for the existing `sp_register_addon`/`sp_dashboard_after_stats` hook patterns, and [[project_overview]] for the live site list.

**Update (2026-07-04): smti-sales pipeline numbers were wrong, now fixed.** Discovered the local `sp_smti_quotes` table only had 5 rows ($300K) — quotes built through the SP Platform's own Quote Builder — while HubSpot's real "Sales Pipeline" (pipeline id `default`) had 50 open deals worth $1,858,674.51 that never touched this table (created via the old legacy SMTI system or directly in HubSpot). Fixed by adding `sp_smti_sales_get_open_pipeline()` to `start-performance-smti-sales.php` (v1.2.8) — queries HubSpot live instead of the local table. Both the new dashboard stat card (smti-sales previously had none) and `sp_smti_sales_intel_summary_lines()` now use this live query; verified via `wp eval` on the live SMTI site that it returns the exact matching numbers (count=50, value=1858674.51). HubSpot pipeline stage IDs for reference: `appointmentscheduled`="Quote Submitted", `contractsent`="Contract Sent", `qualifiedtobuy`="Order Received", `closedwon`/`closedlost`=terminal.

Side note not yet investigated: new quotes created via the SP tool's `create-deal` endpoint default to HubSpot stage `qualifiedtobuy` ("Order Received") when `sp_smti_sales_deal_stage_id` isn't explicitly saved (it currently isn't set on SMTI) — meaning brand-new quotes may start life already at "Order Received" instead of "Quote Submitted". Worth checking if that's intentional.
