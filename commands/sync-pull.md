Pull the latest changes from GitHub for both the Claude Projects folder and the Claude config folder. Run these two git pull commands:

1. Pull claude-projects: `git -C "C:\Users\richa\Documents\Claude Projects" pull`
2. Pull claude-config: `git -C "C:\Users\richa\.claude" pull`

Report the result of each pull — whether it was already up to date, or what files changed. If there are merge conflicts, explain what files are conflicting and ask the user how they want to resolve them.
